name: 'Setup Make'
description: 'Installs a more recent Make.'
author: 'Ian W. Remmel'
inputs:
  version:
    description: 'The version to install'
    default: '4.3'
    required: true
runs:
  using: 'node16'
  main: 'action.js'
