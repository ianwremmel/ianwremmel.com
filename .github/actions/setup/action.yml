name: 'Setup'
description: 'Common setup steps for all jobs'
runs:
  using: 'composite'
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 24
        cache: 'npm'
    - run: npm ci
      shell: bash
    - run: npm run build
      shell: bash
